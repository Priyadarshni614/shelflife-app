# shelflife-app

Project created in the shelflife-app workspace.

## Structure

- `data/`
  - `synthetic_work_signals.json`
- `src/utils/`
  - `creativeAgent.js`

## Setup

1. Add environment variables to `.env`
2. Install dependencies if needed
3. Run the application as appropriate for your project
